<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Active Agent</title>
  <%= csrf_meta_tags %>
  <%#
    Bundles ship prebuilt in the gem (see frontend/)
    and are added to the host app's asset paths by the engine, so mounting the
    dashboard never asks the host to run a JavaScript build.
  %>
  <%= stylesheet_link_tag "action_agent", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "action_agent", type: "module", defer: true %>
</head>
<body class="h-full">
  <%= yield %>
</body>
</html>
