<%#
  The React dashboard's mount point. Initial state rides along as a JSON data
  attribute — no Inertia, no framework requirement on the host app.
%>
<div id="active-agent-dashboard" data-props="<%= props.to_json %>"></div>
