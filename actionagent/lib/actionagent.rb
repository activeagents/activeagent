# frozen_string_literal: true

# The gem is `actionagent`; the code lives under `action_agent`. This is the
# file Bundler requires when it sees `gem "actionagent"` — the same shim Rails
# ships for activerecord, actionpack and friends.
require "action_agent"
